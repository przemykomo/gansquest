[package]
name = "gansquest"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
gansui = {
    path = "../gansui",
    # features = ["debug_ui"]
}
jni = {
    version = "0.22.4",
    #optional = true
}

[lib]
crate-type = ["lib", "dylib"]

[features]
android = [
#"dep:jni"
]
