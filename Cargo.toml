[package]
name = "gansquest"
version = "0.1.0"
edition = "2024"

[dependencies]
gansui = {
    path = "../gansui",
    # features = ["debug_ui"]
}

[lib]
crate-type = ["lib", "dylib"]

[features]
android = []
